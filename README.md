# Nivaes.App.Cross


## Para instalar en mac

Instalar dotnet sdk
```
brew install dotnet-sdk
```

Instalar xcsync tool.

```
dotnet tool install -g dotnet-xcsync --prerelease
```

Generar proyecto xcode

```
xcsync generate
```

Actualizar proyectos xcode
```
xcsync sync
```