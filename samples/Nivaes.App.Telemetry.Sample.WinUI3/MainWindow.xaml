<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Nivaes.App.Telemetry.Sample.WinUI3.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nivaes.App.Telemetry.Sample.WinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Nivaes.App.Telemetry.Sample.WinUI3">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Formulario de Alumnos" FontSize="20" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,20" />

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Nombre:"/>
            <TextBox x:Name="txtNombre" Width="250"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Apellidos:"/>
            <TextBox x:Name="txtApellidos" Width="250"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Edad:"/>
            <TextBox x:Name="txtEdad" Width="100"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Email:"/>
            <TextBox x:Name="txtEmail" Width="250"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0" Spacing="10">
            <Button Content="Guardar" Width="100" Click="Guardar_Click"/>
            <Button Content="Cancelar" Width="100" Click="Cancelar_Click"/>
            <Button Content="Error" Width="100" Click="Error_Click"/>
        </StackPanel>
    </Grid>
</Window>
